name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.REGISTRY_TOKEN }} 
      - uses: actions/checkout@v4
      - name: Build and push the app-python Docker image
        uses: docker/build-push-action@v3
        with:
          context: ./backend/app-python
          push: true
          tags: |
            ghcr.io/avalanche05/app-python:latest
            ghcr.io/avalanche05/app-python:${{ github.sha }}
      # - name: Build Docker image
      #   run: |
      #     docker buildx create --use
          
      #     docker buildx build \
      #       --platform linux/amd64,linux/arm64 \
      #       --tag ghcr.io/${{ github.repository_owner }}/app-python:${{ github.sha }} \
      #       --tag ghcr.io/${{ github.repository_owner }}/app-python:latest \
      #       ./backend/app-python
      # - name: Push Docker image to registry
      #   run: |
      #     docker push ghcr.io/${{ github.repository_owner }}/app-python:latest